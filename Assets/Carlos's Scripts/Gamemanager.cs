using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class Gamemanager : MonoBehaviour
{
    public int collectibles;
    public int DrawStamina;
    static void Main()
    {
        //Player player = new Player();
        //Collectible smallInspiration = new Collectible(20);
        //Collectible largeInspiration = new Collectible(50);

        //player.Draw(30);
        //smallInspiration.Collect(player);
        //player.Draw(40);
        //largeInspiration.Collect(player);
        //player.Draw(50);

        // Start is called before the first frame update
    }
}
